<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple900</item>
        <item name="colorPrimaryDark">@color/purple900</item>
        <item name="colorAccent">@color/purple800</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Detail">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="DataTextAppearanceStyle" parent="Base.TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/darkGray</item>
    </style>

</resources>
